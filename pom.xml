<build>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.8.1</version> <!-- Plugin version -->
            <configuration>
                <source>7</source>  <!-- Java source version -->
                <target>7</target>  <!-- Java target version -->
            </configuration>
        </plugin>
    </plugins>
</build>
